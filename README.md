# x-ray-weapon-detector

Built a website that allows the user to upload an X-ray image.
An AI then detects possible threats (gunns, knives, etc) in the image along with bounding boxes around the objects.
